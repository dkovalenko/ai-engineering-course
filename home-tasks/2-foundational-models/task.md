# Домашнє завдання №2: Foundational Models & Sampling Parameters

## Мета завдання
Навчитися на практиці керувати поведінкою моделей за допомогою параметрів семплювання (`Temperature`, `Top-P`) та порівняти можливості великих хмарних моделей (Foundational) із локальними (On-device/SLM).

---

## 🛠 Інструментарій
* **Large Model:** Використовуйте будь-яку потужну модель через [OpenRouter](https://openrouter.ai/) (наприклад, `gpt-4o`, `claude-3-5-sonnet`, `gemini-1.5-pro`).
* **Small/Local Model:** Використовуйте маленькі/відкриті моделі через OpenRouter (наприклад, `gemma2:2b`, `llama3.1:8b` або `phi3`). (Ви також можете запустити модель локально)

---

## Опис експерименту

Для кожного з кейсів(оберіть буь-які 2) нижче вам потрібно виконати **6 запусків**:
1. Large Model при T=0.1, 0.7, 1.2.
2. Small Model при T=0.1, 0.7, 1.2.

**Загальні налаштування для всіх тестів:**
* `Top-P`: **0.9** (фіксовано).
* `Seed`: **42** (для відтворюваності результатів).

---

## 📂 Кейси для тестування

### Кейс 1: Data Extraction (Тест на точність)
**Завдання:** Витягни дані з лога у форматі JSON. 
**Очікувані поля:** `timestamp`, `status_code`, `request_time_ms`, `error_type`, `client_ip`.

**Промпт:**
> "Extract the following fields into a valid JSON: timestamp, status_code, request_time_ms, error_type, client_ip. If a field is missing, use null."

**Контекст (Лог):**
```text
2026-02-27 14:10:05.123 INFO  192.168.1.15 GET /api/v1/user/profile 200 45ms
2026-02-27 14:10:06.841 WARN  192.168.1.15 GET /api/v1/auth/login 401 12ms (invalid_token)
2026-02-27 14:10:08.002 ERROR 10.0.5.102 POST /api/v1/orders/create 500 1500ms (db_connection_timeout)
2026-02-27 14:10:12.445 DEBUG 172.16.0.44 GET /health 200 2ms
2026-02-27 14:10:15.901 FATAL 192.168.1.88 POST /api/v2/payment/process 499 30000ms (upstream_reset_before_response)
```

### Кейс 2: Text Summarization (Стислий підсумок)
Завдання: Зроби стислий технічний підсумок тексту у форматі 5 bullet points українською мовою.
Промпт: Зроби стислий технічний підсумок тексту у форматі 5 bullet points українською мовою. Зосередься на причинах виникнення проблеми та методах її вирішення.

Контекст (Текст про галюцинації):
```text
Галюцинації в сучасних мовних моделях (LLM) виникають через те, що вони за своєю природою є статистичними предикторами наступного токена, 
а не логічними базами даних. Модель не "знає" фактів, вона лише обчислює найімовірнішу послідовність символів. 
Основною причиною помилок є застарілі дані (data cut-off) та відсутність механізму перевірки тверджень у реальному часі. 
Для мінімізації цього ефекту в AI Engineering використовують архітектуру RAG (Retrieval-Augmented Generation), 
яка "приземлює" відповіді моделі на конкретні документи. 
Також важливо контролювати параметр Temperature: при значеннях понад 1.2 модель починає надавати надмірну вагу малоймовірним токенам, 
що призводить до вигадування неіснуючих фактів. 
Дослідження показують, що впровадження RAG разом із системними промптами на кшталт "відповідай лише на основі тексту" 
знижує рівень галюцинацій на 60-70% у порівнянні зі звичайним чатом.
```

### Кейс 3: Складний Q&A (Reasoning & Extraction)
Завдання: Обчисли підсумкову вартість та визнач статус акаунта на основі заданих умов.
Промпт: > «Дай відповідь на основі тексту:

Якою буде вартість зберігання 120 ГБ даних для акаунта типу 'Academic', якщо він обрав річну підписку?

Чи буде такий акаунт заблоковано, якщо він завантажить 5 терабайт даних за один день?
Відповідь має бути короткою, з посиланням на конкретні пункти правил.»

Контекст (Правила хмарного сховища 'Nebula-V'):

```text
1. Типи акаунтів: > Користувачі 'Standard' мають ліміт 10 ГБ. Акаунти 'Academic' (верифікація через .edu) отримують 50 ГБ безкоштовно.

2. Тарифікація понад ліміт: > Кожен додатковий ГБ коштує $0.50/міс. При виборі річної підписки діє знижка 20% на весь обсяг додаткового зберігання. ВАЖЛИВО: Для акаунтів 'Academic' знижка на річну підписку становить лише 10%, але вона додається до базової студентської знижки у $2 на загальний рахунок.

3. Політика безпеки (Fair Use): > Денний ліміт завантаження (Ingress) становить 1 ТБ для всіх типів акаунтів. Якщо обсяг завантаження перевищує ліміт у 3 рази протягом доби, акаунт переходить у статус 'Pending'. Якщо перевищення складає понад 5 разів — акаунт блокується негайно без права апеляції.

4. Спеціальні умови: > Студенти (Academic) можуть подати запит на збільшення ліміту Ingress до 10 ТБ, але ця опція активується лише через 48 годин після верифікації карти. До цього моменту діють загальні правила безпеки (п. 3).
```


### Кейс 4: Creative Pitch (Тест на варіативність)
Завдання: Напиши короткий рекламний опис (3-4 речення) для CLI-інструменту.

Промпт:

"Write a catchy 3-sentence marketing pitch for a developer tool named 'Git-Burn'. Its main function is to automatically delete git branches that haven't been touched for 30 days and send a Slack notification to the last committer. Target audience: lazy DevOps engineers."


### Бонусне завдання (The Nucleus Effect)

Візьміть Кейс 2 (Creative Pitch), зафіксуйте T=1.0 та порівняйте два запуски: Top-P = 1.0 Top-P = 0.1
Запитання: Як обмеження "ядра" ймовірностей (Top-P) вплинуло на творчість моделі? 
Опишіть різницю одним реченням.
