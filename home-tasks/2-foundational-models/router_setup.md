
## 🛠 Налаштування OpenRouter для курсу
OpenRouter — це агрегатор, який надає доступ до майже всіх існуючих LLM (GPT-4, Claude 3, Gemini, Llama 3) через один уніфікований API-інтерфейс. 
Це зручно, щоб не плодити десятки акаунтів.

# Реєстрація та ключ
1. Перейди на openrouter.ai.
2. Авторизуйся через Google або GitHub.
3. Зайди в розділ Keys та натисни "Create Key".
4. Важливо: Скопіюй ключ і збережи його в надійному місці (наприклад, у менеджері паролів). Він показується лише один раз.

# Поповнення балансу
OpenRouter працює за моделлю Prepaid:
1. Зайди в Credits.
2. Для виконання всіх домашніх завдань курсу тобі вистачить $10. Багато моделей (наприклад, деякі версії Llama або Gemma) на OpenRouter взагалі безкоштовні, але для доступу до топових моделей (GPT-4o) потрібен невеликий баланс.

## Налаштування Ollama (Local LLM) - альтернатива openrouter для малих моделей якщо є залозі щоб запускати самому

1. Встановлення
Завантаж інсталятор для своєї ОС із ollama.com.

Встанови програму. Якщо ти на Windows/Mac, у треї з'явиться іконка Ollama (це означає, що локальний сервер запущено).

2. Завантаження моделей
Для домашнього завдання нам знадобляться "легкі" моделі, які підуть навіть на звичайному ноутбуці. Відкрий термінал і виконай:

```Bash
# Найкраща модель у категорії "маленьких"
ollama pull gemma2:2b
```

# Класика від Meta (потребує мінімум 8 ГБ оперативки)
`ollama pull llama3.1:8b`
Щоб перевірити, чи все працює, можеш просто написати в терміналі:
`ollama run gemma2:2b` і почати чатитись. Щоб вийти, напиши /exit.
